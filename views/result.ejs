<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Hasil Jawaban</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <div class="container">
        <% if (isFinished) { %>
            <h2>🎉 Kuis Selesai! 🎉</h2>
            <p>**<%= studentName %>**, Anda telah menyelesaikan semua **<%= totalQuestions %>** soal.</p>
            <p>Hasil Anda telah disimpan. Silakan kembali ke halaman utama.</p>
            <a href="/pembelajaran">Kembali ke Halaman Awal</a>
        <% } else { %>
            <% if (isCorrect) { %>
                <h2 style="color: green;">✅ Jawaban Anda **BENAR**!</h2>
                <p>Kunci Jawaban: **<%= correctAnswer %>**</p>
            <% } else { %>
                <h2 style="color: red;">❌ Jawaban Anda **SALAH**!</h2>
                <p>Kunci Jawaban yang Benar adalah: **<%= correctAnswer %>**</p>
            <% } %>
            
            <form action="/quiz" method="GET">
                <button type="submit">Lanjut ke Soal **<%= nextQId %>**</button>
            </form>
        <% } %>
    </div>
</body>
</html>